int there_is_obstacle(); //Made by Pawel
                         //This function returns 1 when the distance value from the ultrasonic sensor is smaller than DETECTION_DISTANCE global variable.
int what_kind_of_obstacle();//Made by Pawel
                         //This function returns 1 when the recognized color is red, for movable obstacle.
int detection();//Made by Pawel
                         //This function is a combination of previous ones to move the movable obstacle behind the robot.
int distance_to_obstacle(); //Made by Pawel
                         // This function is necessary to return the distance to obstacle. It was used for scan functions.
